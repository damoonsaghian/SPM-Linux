# lock screen
# swapps lock mode: ask for password
# codev lock mode: read only access to public projects
# 	external storage devices will be writable
# 	when one tries to edit readonly files, or when "escape" is pressed in the root window, show swapps

# workspace "codev"

# sway goes to "lock" mode with its own set of keybindings
# when "alt+tab" is pressed, show swapps

# if an empty password is entered, or "escape" is pressed, show codev

# during boot, the user will be automatically logged in, and the lock screen will be activated

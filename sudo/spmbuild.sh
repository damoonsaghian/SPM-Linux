project_dir="$(dirname "$0")"

# sudo service: listen to dbus messages
# sudo client: send dbus messages

# console level keybinding: when "F5-8" is pressed, switch to virtual terminal 8, if a program is running there
